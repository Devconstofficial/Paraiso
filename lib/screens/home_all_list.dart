import 'package:flutter/material.dart';

class HomeAllList extends StatelessWidget {
  const HomeAllList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
